version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: app
      APP_PORT: 3000

  app:
    build: .
    restart: unless-stopped
    volumes:
      - ${APP_DATA_DIR:-./data}:/app/data
    labels:
      - "umbrel.app-id=solar-miner"
